name: kiko
description: TUI library for building terminal user interfaces.
version: 0.0.1
publish_to: "none"

environment:
  sdk: ^3.10.3

resolution: workspace

# Add regular dependencies here.
dependencies:
  cassowary: ^0.4.3
  characters: ^1.3.0
  collection: ^1.19.1
  kiko_log: ^0.0.1
  meta: ^1.16.0
  termlib: ^0.4.0
  termparser: ^0.3.0
  termunicode: ^0.1.0

dev_dependencies:
  coverage: ^1.11.0
  lints: ^6.0.0
  test: ^1.24.0
  very_good_analysis: ^10.0.0
