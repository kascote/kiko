/// An enumeration representing different types of alignment options.
///
/// This enum can be used to specify the alignment of widgets within a layout.
/// Each value in the enum corresponds to a specific alignment strategy.
enum Alignment {
  /// Align element to the left
  left,

  /// Align element to the center
  center,

  /// Align element to the right
  right,
}
