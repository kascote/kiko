name: plume
description: Flutter-style, solver-free layout engine for cell grids (TUI). Geometry only.
version: 0.0.1
publish_to: "none"

environment:
  sdk: ^3.11.1

resolution: workspace

dependencies:
  meta: ^1.16.0

dev_dependencies:
  coverage: ^1.11.0
  lints: ^6.0.0
  test: ^1.24.0
  very_good_analysis: ^10.0.0
